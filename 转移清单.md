# 界面操作收集装置 —— 完整转移清单

> 生成时间：2026-09-08 | 场景：从 C 盘整体转移到其他盘
> 本文件为**唯一权威清单**，覆盖代码、配置、数据、工具、学习探索全部内容

---

## 一、项目概览

| 项 | 值 |
|---|---|
| 名称 | 界面操作收集装置 |
| 用途 | 从桌面期货软件（Legend/同花顺）+ REST API（OpenVLab/交易可查）采集行情数据 |
| Python | 3.12（原路径 `D:\Python\python.exe`） |
| 测试 | pytest 119/119 + selftest 16/16 |
| 数据源 | 4 源：Legend(CDP)、同花顺(UIA+OCR)、OpenVLab(REST)、交易可查(REST) |
| 原位置 | `C:\Users\Lenovo\Desktop\界面操作收集装置` |
| 总大小 | 299 MB（含 venv）/ 44 MB（排除 venv/pycache/zcode） |

---

## 二、目录结构（完整树形）

```
界面操作收集装置/                     ← 目标盘根目录，直接复制此文件夹
│
├── 核心代码（9 个 .py，157 KB）
│   ├── run.py                    入口：--probe / --once / --daemon / --serve / --selftest
│   ├── collector_base.py         共享采集器循环基座 run_collector_loop()
│   ├── ui_collector.py           原语层：CDP+UIA适配器、剪贴板(ctypes)、截图、RapidOCR
│   ├── legend_ui_collector.py    Legend采集（CDP text5解析+自动登录）
│   ├── ths_ui_collector.py       同花顺采集（UIA→OCR语义解析+行情Tab切换+窗口恢复）
│   ├── openvlab_collector.py     OpenVlab REST补充源（三端点并行 ThreadPoolExecutor）
│   ├── jiaoyikecha_collector.py  交易可查REST第四数据源（含net_position净持仓）
│   ├── fusion.py                 协同层：sys.path注入量化模块、monitor.db、质量护栏、StatusHub
│   └── dashboard.py              HTML显示页（自包含，5s自动刷新，启动时写一次）
│
├── 配置文件
│   ├── device_config.py          config.json+selectors.json加载、AES解密、legend_credentials()
│   ├── config.json               数据源/路径/账号/质量参数配置
│   ├── selectors.json            控件/提取方式配置
│   ├── requirements.txt          依赖声明（核心标准库，仅pytest测试依赖）
│   └── .env.example              环境变量模板（可选，默认值见config.json）
│
├── 启动脚本
│   ├── Legend 调试模式.bat        以CDP调试口启动Legend（含 --remote-allow-origins=*）
│   └── 打开界面采集.bat           一键 run.py --daemon 常驻四源采集
│
├── 文档
│   ├── README.md                 快速使用说明
│   ├── 上下文摘要.md             完整开发历史（§一~§十七，39KB）
│   └── 转移清单.md               本文件
│
├── 测试（119 个用例）
│   └── tests/（7 个 .py）
│       ├── test_dashboard.py         4 用例
│       ├── test_fusion.py            41 用例
│       ├── test_jiaoyikecha.py       11 用例
│       ├── test_legend_ui.py         13 用例
│       ├── test_openvlab.py          9 用例
│       ├── test_ths_ui.py            23 用例（含 is_quote_like/parse_quote_ocr_row/variety清理）
│       └── test_ui_collector.py      18 用例
│
├── data/                            运行时数据（可再生，转移后自动重建）
│   ├── collector_status.json        采集状态快照
│   ├── report_device.json           设备报告
│   ├── device.log                   运行日志
│   └── openvlab_surfaces/           20个品种波动率曲面快照
│
├── 网页学习探索/（292 MB）
│   ├── 01_jiaoyikecha/（193 KB）    学习笔记 + HTML归档 + jykt_*.json
│   ├── 02_openvlab_market/（7.3 MB） 学习笔记 + raw/36个API JSON + api_archive
│   ├── 03_qhqqbk/（29 MB）          学习笔记 + 182个HTML归档 + qhqqbk_links.json
│   ├── 综合速查手册.md              国内数据源实测记录（12 KB）
│   └── 工具/（255 MB）              venv(255MB) + 8个Python脚本
│
├── 软件学习探索/（6.5 MB）
│   ├── 01_Legend/                   元素地图8个JSON + DOM归档7个HTML
│   ├── 02_同花顺/                   探索报告 + 探索结果
│   ├── 03_调研资料/                 openvlab_api_存档 30+个JSON
│   ├── 04_截图归档/                 12张Legend截图
│   └── 工具/                       enum_ui.py, legend_map.py, openvlab_api.py
│
└── 隐藏文件
    ├── .env.example
    └── .zcode/plans/（2个plan md）
```

---

## 三、⚠️ 转移必须解决的硬编码路径（共 20+ 处）

> **这是转移失败的第一大原因**，必须逐项处理

### 3.1 `config.json`（必须修改，6 处）

```json
{
  "quant_dir": "C:\\Users\\Lenovo\\Desktop\\量化\\futures_monitor",
  "legend": {
    "exe": "E:\\OpendVlab Legend\\openvlab-legend\\openvlab-legend.exe",
    "data_dir": "E:\\OpendVlab Legend\\openvlab-legend\\openvlab-legend-data",
    "appdata_dir": "C:\\Users\\Lenovo\\AppData\\Roaming\\openvlab-legend"
  },
  "ths": {
    "exe": "E:\\同花顺期货通\\bin\\happ.exe",
    "notice_xml": "E:\\同花顺期货通\\bin\\FutureData\\Notice.xml"
  }
}
```

**修改方式**：逐字段改为目标机器实际路径。若 Legend/同花顺仍装在 E 盘则不动；若未安装则置空字符串。

### 3.2 `device_config.py` 第19-30行（必须修改，6 处！）

```python
_DEFAULTS = {
    "quant_dir": r"C:\Users\Lenovo\Desktop\量化\futures_monitor",   # ← 改
    "legend": {
        "exe": r"E:\OpendVlab Legend\openvlab-legend\openvlab-legend.exe",    # ← 改
        "data_dir": r"E:\OpendVlab Legend\openvlab-legend\openvlab-legend-data",  # ← 改
        "appdata_dir": r"C:\Users\Lenovo\AppData\Roaming\openvlab-legend",  # ← 改
    },
    "ths": {
        "exe": r"E:\同花顺期货通\bin\happ.exe",        # ← 改
        "notice_xml": r"E:\同花顺期货通\bin\FutureData\Notice.xml",  # ← 改
    },
}
```

> ⚠️ 即使 `config.json` 已修改，`_DEFAULTS` 是兜底默认值，`load_config()` 执行 `_deep_merge(_DEFAULTS, config.json内容)`。**两处必须同时修改**，否则 config.json 缺字段时会回退到旧路径。

### 3.3 `Legend 调试模式.bat`（必须修改，1 处）

```batch
start "" "E:\OpendVlab Legend\openvlab-legend\openvlab-legend.exe" --remote-debugging-port=9225 --remote-allow-origins=*
```
→ 修改 `E:\OpendVlab Legend\...` 为目标机器实际路径

### 3.4 `打开界面采集.bat`（必须修改，1 处）

```batch
"D:\Python\python.exe" run.py --daemon
```
→ 修改 `D:\Python\python.exe` 为目标机器 Python 路径

### 3.5 `.env.example`（必须修改，1 处）

```
FUTURES_MONITOR_QUANT_DIR=C:\Users\Lenovo\Desktop\量化\futures_monitor
```
→ 若量化项目路径变化则修改；无 `.env` 文件时不生效，但 `.env.example` 需与实际一致

### 3.6 `网页学习探索/工具/venv/pyvenv.cfg`（venv 无法激活！）

```
home = D:\Python
executable = D:\Python\python.exe
command = D:\Python\python.exe -m venv C:\Users\Lenovo\Desktop\界面操作收集装置\网页学习探索\工具\venv
```

**解决方案**（二选一）：

**方案A（推荐）**：转移时删除 venv，转移后重新创建：

```bash
cd 网页学习探索\工具
python -m venv venv
venv\Scripts\pip install scrapling playwright patchright curl_cffi browserforge msgspec
```

**方案B**：手动修改 pyvenv.cfg 将路径改为目标机器：

```
home = <目标机器Python安装目录>
executable = <目标机器Python.exe路径>
command = <目标机器Python.exe路径> -m venv <目标venv路径>
```

并重新安装 pip：`venv\Scripts\python -m ensurepip`

### 3.7 `README.md`（文档类，3 处，不修改可运行但文档不准确）

- 第4行引用 `量化` 项目
- 第40行引用 `量化\futures_monitor\CHANGELOG.md`
- 第58行引用 `D:\Python\python.exe`

### 3.8 `上下文摘要.md`（历史文档，大量路径，一般不修改）

历史记录性质，包含 `C:\Users\Lenovo\Desktop\...` 等路径。属历史快照，可保留原样。

### 3.9 `网页学习探索/综合速查手册.md`（1 处）

引用 `D:\Python\python.exe` 工具命令示例

### 3.10 `软件学习探索/工具/` 脚本（3 处，文档性质）

- `cdp_explore.py`、`explore_nav.py`、`enum_ui.py`、`legend_map.py`：shebang 行写 `D:/Python/python.exe`

---

## 四、Python 解释器依赖

本项目使用 Python 3.12，原解释器路径 `D:\Python\python.exe`。

**转移后必须**：

| 情况 | 操作 |
|------|------|
| 目标机器 Python 装在相同路径 | 无需操作 |
| 目标机器 Python 在其他路径 | 修改 `打开界面采集.bat` 中路径 |
| 目标机器未装 Python | 安装 Python 3.12，记住安装路径 |

**批量替换 bat 中的 Python 路径**（推荐用文本编辑器替换）：

```bash
# 在目标机器编辑
"打开界面采集.bat"      # 改 D:\Python\python.exe 为实际路径
"Legend 调试模式.bat"   # 改 E:\ 路径
```

---

## 五、量化项目协同路径（关键！）

`fusion.py` 第44行检查量化项目路径：

```python
qdir = Path(device_config.quant_dir())
if not (qdir / "storage.py").exists():
    raise RuntimeError("量化目录未找到: %s" % qdir)
```

**且** `config.json` 和 `device_config.py` 都包含量化项目路径 `C:\Users\Lenovo\Desktop\量化\futures_monitor`。

### 转移后必须处理

| 场景 | 操作 |
|------|------|
| 量化项目与本项目在同一目录下 | 改 `quant_dir` 为新相对路径的绝对路径 |
| 量化项目在不同机器 | 需要量化项目同步转移，并改 `quant_dir` |
| 量化项目未转移 | 装置**无法运行**（fusion.py 不可用，但 data/dashboard 仍可单独使用） |
| 量化项目在目标机器存在但路径不同 | 改 `config.json` + `device_config.py` |

### 验证命令（转移后）

```bash
D:\Python\python.exe run.py --selftest
# 若 fusion 报错：量化目录未找到 → 需修改 config.json 的 quant_dir
```

---

## 六、venv 转移细节

### venv 内容清单

```
网页学习探索/工具/venv/（255 MB）
├── pyvenv.cfg          ⚠️ 含绝对路径（见 §3.6）
├── Scripts/
│   ├── python.exe      Python 解释器副本（PE 文件，无硬编码路径，可迁移）
│   ├── pip.exe         pip 可执行文件（PE 文件，无硬编码路径，可迁移）
│   ├── pip3.exe / pip3.12.exe
│   ├── activate / activate.bat / activate.ps1
│   └── scrapling / playwright / patchright 等命令
├── Lib/site-packages/  安装的包（无绝对路径引用）
│   ├── scrapling-0.4.15/
│   ├── playwright/
│   ├── patchright/
│   ├── curl_cffi/
│   ├── browserforge/
│   └── msgspec/
```

### venv 可迁移性分析

| 文件 | 含硬编码路径 | 影响 |
|------|-------------|------|
| pyvenv.cfg | ✅ 含解释器绝对路径 | **激活失败**（见 §3.6） |
| Scripts/*.exe | ❌ PE 文件无路径 | 可迁移 |
| Lib/site-packages/* | ❌ 无路径引用 | 可迁移 |
| Scripts/activate.bat | ❌ 无路径引用 | 可迁移 |

**结论**：venv **不能直接复制到其他盘使用**，pyvenv.cfg 会导致 `Scripts\activate` 失败。推荐方案：删掉重装（5分钟）。

### 重建 venv 命令

```bash
cd 网页学习探索\工具
# 删除旧 venv
rmdir /s /q venv
# 重建
python -m venv venv
venv\Scripts\pip install scrapling playwright patchright curl_cffi browserforge msgspec
```

---

## 七、转移操作完整清单

### 转移前（当前机器）

```bash
# 1. 验证当前状态
cd C:\Users\Lenovo\Desktop\界面操作收集装置
D:\Python\python.exe -m pytest tests/ -q        # 期望 119 passed
D:\Python\python.exe run.py --selftest           # 期望 16/16

# 2. 清理可再生文件（减小转移包约 260 MB）
rmdir /s /q __pycache__
rmdir /s /q .pytest_cache
rmdir /s /q .zcode
rmdir /s /q 网页学习探索\工具\venv              # 255 MB，转移后重建
rmdir /s /q data                                # 运行时数据，重新采集生成
del @AutomationLog.txt                          # UIA 自动化日志，非核心

# 3. 记录当前核心文件哈希（转移后比对）
certutil -hashfile run.py MD5
certutil -hashfile config.json MD5
certutil -hashfile device_config.py MD5
```

清理后大小：约 **44 MB**（可进一步压缩为 zip）。

### 复制到目标盘

```bash
# 使用 robocopy（保留文件时间戳）
robocopy "C:\Users\Lenovo\Desktop\界面操作收集装置" "<目标盘>:\界面操作收集装置" /E /XD __pycache__ .pytest_cache .zcode venv /XF @AutomationLog.txt *.pyc

# 或打包 zip 后解压
```

### 转移后（目标机器）—— 必做

```bash
cd <目标盘>:\界面操作收集装置

# ===== 第1步：修改 Python 路径 =====
# 编辑"打开界面采集.bat"：改 D:\Python\python.exe 为目标机器实际路径

# ===== 第2步：修改配置文件（6 处路径）=====
# 编辑 config.json：修改所有绝对路径
# 编辑 device_config.py 第19-30行：修改 _DEFAULTS 中所有绝对路径
# 编辑".Legend 调试模式.bat"：修改 Legend exe 路径

# ===== 第3步：重建 venv =====
cd 网页学习探索\工具
python -m venv venv
venv\Scripts\pip install scrapling playwright patchright curl_cffi browserforge msgspec

# ===== 第4步：验证 =====
cd <目标盘>:\界面操作收集装置
<目标python> -m pytest tests/ -q                 # 期望 119 passed
<目标python> run.py --selftest                    # 期望 16/16
<目标python> run.py --once                        # 单次采集测试
```

### 转移后 —— 按需修改

```bash
# 若量化项目路径变化
# 编辑 config.json：改 quant_dir
# 编辑 device_config.py：改 _DEFAULTS["quant_dir"]

# 若 Legend/同花顺未安装在 E 盘
# 编辑 config.json：改 legend/ths 路径（或置空）
# 编辑 device_config.py：改 _DEFAULTS 中对应路径

# 若需要 .env（可选）
# 从 .env.example 复制为 .env，修改路径
```

---

## 八、可再生 vs 不可再生文件

### 可再生（转移丢失后可重新生成）

| 文件/目录 | 重建方式 | 预计时间 |
|-----------|----------|----------|
| `data/` 全部 | `run.py --once` 重新采集 | 1 分钟 |
| `__pycache__/` / `.pytest_cache/` | pytest 自动重建 | 秒 |
| `网页学习探索/工具/venv/` | pip install 重建 | 5 分钟 |
| `qhqqbk_目录树.md` | `rebuild_qhqqbk.py` | 5 秒 |
| `@AutomationLog.txt` | 自动产生 | - |

### 不可再生（转移时务必保留）

| 文件/目录 | 原因 | 大小 |
|-----------|------|------|
| 全部 9 个核心 .py | 原始开发成果 | 157 KB |
| `tests/` 7个测试文件 | 119 用例 | 45 KB |
| `上下文摘要.md` | 17轮迭代历史 | 39 KB |
| `转移清单.md` | 本文件 | - |
| `config.json` + `selectors.json` | 配置定型 | 2 KB |
| `.bat` 启动脚本 | 配置定型 | 1 KB |
| `.env.example` | 环境变量模板 | 654 B |
| `README.md` | 快速使用说明 | 4 KB |
| `网页学习探索/03_qhqqbk/HTML归档/` | 部分用 scrapling 重试获取 | 29 MB |
| `网页学习探索/03_qhqqbk/数据/` | 含 `qhqqbk_main.html` 解析源 | 100 KB |
| `网页学习探索/01_jiaoyikecha/数据/` | jykt_*.json + jiaoyikecha.db | 50 KB |
| `网页学习探索/02_openvlab_market/raw/` | 36 个 API 响应 JSON（含曲面） | 5 MB |
| `网页学习探索/综合速查手册.md` | 国内数据源实测记录 | 12 KB |
| `软件学习探索/` 全部 | 软件学习成果 | 6.5 MB |
| 各学习笔记 .md | 知识积累 | 100 KB |

---

## 九、核心文件哈希（转移后校验用）

| 文件 | 大小(B) | MD5（前12位） |
|------|---------|---------------|
| run.py | 12,587 | `aa3259fc7b22` |
| fusion.py | 19,684 | `f84c07c68543` |
| ui_collector.py | 18,614 | `8cf510fb55e7` |
| ths_ui_collector.py | 16,105 | `7d28bf1ff6f8` |
| openvlab_collector.py | 23,260 | `c7af081a40e6` |
| legend_ui_collector.py | 14,105 | `94aca8ebfca8` |
| jiaoyikecha_collector.py | 14,892 | `cf709e189275` |
| dashboard.py | 17,643 | `0e48372b5f88` |
| device_config.py | 5,297 | `b002d883e06e` |
| collector_base.py | 1,923 | `dc147c22c5ea` |
| config.json | 1,185 | `45b7a18b16a6` |
| selectors.json | 848 | `c752d2f0d497` |

**转移后校验**：

```bash
for f in run.py fusion.py ui_collector.py ths_ui_collector.py openvlab_collector.py \
         legend_ui_collector.py jiaoyikecha_collector.py dashboard.py device_config.py \
         collector_base.py config.json selectors.json; do
  certutil -hashfile "$f" MD5 | head -2
done
```

哈希相同 = 文件完整；哈希不同 = 文件损坏。

---

## 十、常见转移问题 FAQ

### Q1：转移后运行 `run.py --selftest` 报"量化目录未找到"

**原因**：`device_config.py` 中 `_DEFAULTS` 的 `quant_dir` 还是旧路径，或 `config.json` 的 `quant_dir` 没改。

**解决**：
1. 编辑 `config.json`，修改 `quant_dir` 为量化项目实际路径
2. 编辑 `device_config.py` 第20行，修改 `_DEFAULTS["quant_dir"]` 为同一路径

### Q2：转移后 `打开界面采集.bat` 双击无反应

**原因**：Python 路径 `D:\Python\python.exe` 在目标机器不存在。

**解决**：编辑 bat 文件，将 Python 路径改为目标机器实际路径。

### Q3：转移后 `网页学习探索/工具/venv` 激活失败（Scripts\activate 报错）

**原因**：`pyvenv.cfg` 含旧解释器绝对路径，venv 无法定位 Python。

**解决**：
```bash
cd 网页学习探索\工具
rmdir /s /q venv
python -m venv venv
venv\Scripts\pip install scrapling playwright patchright curl_cffi browserforge msgspec
```

### Q4：转移后 `--once` 跑成功但 `collector_status.json` 空

**原因**：Legend/同花顺未打开，或路径配置错误导致无法检测窗口。

**解决**：先 `--probe` 检查各数据源在线状态，按提示操作。

### Q5：转移后 dashboard 打不开（8790 端口无响应）

**原因**：dashboard HTML 只在采集时写入，转移后 `data/` 为空（被清理了）。

**解决**：先运行 `--once` 或 `--daemon`，dashboard.html 会在 data/ 中自动生成。

### Q6：中文路径是否安全

**本项目测试通过**：Python 3.12 + Windows 10/11 + 中文目录名完全兼容，无需特殊处理。

### Q7：需要同时转移量化项目吗

**必须**：`fusion.py` 依赖量化项目的 `storage.py`、`config.py`、`option_chain.py` 等模块。若量化项目不在 `quant_dir` 指向的路径，装置自检报错。

### Q8：如果只转移核心代码（不转移学习探索/工具）可以吗

**可以**。核心运行只需：
- 9 个 .py + config.json + selectors.json + .env.example + 2 个 .bat + README.md
- 共约 130 KB
- 数据/测试/学习探索均为可选

### Q9：`@AutomationLog.txt` 是什么，能否删除

UIA 自动化库生成的日志（`TabItemControl BoundingRectangle is (0,0,0,0)`），非核心，可删除。

### Q10：转移后 `legend_credentials()` 报错找不到凭据文件

**原因**：`legend.appdata_dir` 指向 `C:\Users\Lenovo\AppData\Roaming\openvlab-legend`，该路径包含 Login 凭据。

**解决**：
- 若 Legend 重新安装在目标机器，登录一次 SimNow 即会自动生成凭据文件
- 或在 `config.json` 的 `ctp_accounts` 中显式配置账号密码（无需读文件）

---

## 十一、文件总清单（按重要性排序）

| 优先级 | 文件 | 必须转移 | 转移后需修改 |
|--------|------|----------|-------------|
| **P0** | 9 个核心 .py | ✅ | ❌ |
| **P0** | config.json | ✅ | **必须改 6 处路径** |
| **P0** | device_config.py | ✅ | **必须改 6 处 _DEFAULTS** |
| **P0** | selectors.json | ✅ | ❌ |
| **P1** | 打开界面采集.bat | ✅ | **必须改 Python 路径** |
| **P1** | Legend 调试模式.bat | ✅ | **必须改 Legend 路径** |
| **P1** | .env.example | ✅ | 按需改 quant_dir |
| **P2** | tests/（7 个测试文件） | ✅ | ❌ |
| **P2** | README.md | ✅ | ❌（文档性质） |
| **P2** | 上下文摘要.md | ✅ | ❌（历史记录） |
| **P2** | 转移清单.md | ✅ | ❌ |
| **P3** | 网页学习探索/03_qhqqbk/HTML归档/ | 可选 | ❌（29 MB） |
| **P3** | 网页学习探索/数据/各文件 | 可选 | ❌ |
| **P3** | 软件学习探索/全部 | 可选 | ❌ |
| **P4** | data/ | 无需 | 重新采集生成 |
| **P4** | 网页学习探索/工具/venv/ | 无需 | 重建（5分钟） |
| **P4** | @AutomationLog.txt | 无需 | ❌ |

---

## 十二、⚠️ C 盘原代码删除后必须注意的风险

> **核心原则**：C 盘删除后，`device_config.py` 中的 `_DEFAULTS` 成为唯一的兜底路径源，无法再"退回旧路径"验证。转移后必须在删除前**完整测试通过**，不能依赖任何 C 盘残留。

### 12.1 删除前必须完成的验证（按顺序）

```
1. 转移到目标盘
2. 修改 config.json（6处路径）
3. 修改 device_config.py _DEFAULTS（6处路径）
4. 修改 2个 .bat（Python路径 + Legend路径）
5. 重建 venv
6. 运行 pytest → 119 passed
7. 运行 selftest → 16/16
8. 运行 --once 单次采集 → 验证数据正常
9. 以上全部通过后，才能删除 C 盘代码
```

### 12.2 C 盘删除后无法再做的事

| 事项 | 影响 | 解决方式 |
|------|------|----------|
| Legend 自动登录 | `legend_credentials()` 读 C 盘 `AppData\Roaming\openvlab-legend\ctp-login-credentials.json`，该文件在 C 盘用户目录下，不在项目目录内 | 重新登录 SimNow 自动生成，或在 `config.json` 的 `ctp_accounts` 中配置账号 |
| 量化项目 sys.path 注入 | fusion.py 第45行 `sys.path.insert(0, qdir)`，若量化项目不在 `quant_dir` 则 import 失败 | 确保量化项目已在目标机器，`quant_dir` 指向正确位置 |
| 量化报告写入 | fusion.py 第447行向 `qdir/reports/device_status.txt` 写入，量化项目不存在则静默失败 | 同上 |
| 参考旧代码 | 无法再打开 C 盘代码查看实现细节 | **转移前导出关键笔记**（已含上下文摘要.md） |
| 恢复旧配置 | config.json/device_config.py 改错后无法从 C 盘拷回旧版 | **转移前备份原始文件**（cp config.json config.json.bak） |

### 12.3 C 盘删除后可能的连锁故障

#### 故障1：Legend 提示"无可用凭据，需手动登录"

**现象**：`_check_login()` 检测到登录页，尝试自动重登但 `legend_credentials()` 返回空列表。

**原因**：Legend 的凭据文件在 `C:\Users\Lenovo\AppData\Roaming\openvlab-legend\ctp-login-credentials.json`（用户 AppData 目录），不在本项目目录内。新机器上 Legend 首次登录后会自动生成。

**解决**：在 Legend 中手动登录一次 SimNow 即可。或在 `config.json` 中配置：
```json
"ctp_accounts": [{"site_id": "simnow", "user_id": "你的账号", "password": "你的密码"}]
```

#### 故障2：selftest 报"量化目录未找到"

**现象**：`fusion.py` `ensure_quant()` 抛 RuntimeError。

**原因**：`quant_dir` 指向的量化项目路径不存在。

**解决**：
1. 确认量化项目已转移到目标机器
2. 修改 `config.json` 的 `quant_dir` + `device_config.py` 的 `_DEFAULTS["quant_dir"]`
3. 验证：`Path(config.json中的quant_dir) / "storage.py"` 文件存在

#### 故障3：--once 采集成功但 Dashboard 无数据

**现象**：`http://127.0.0.1:8790/dashboard.html` 显示空白或旧数据。

**原因**：data/ 目录为空（转移时清理了，或从未采集过），dashboard.html 未生成。

**解决**：运行 `--once` 或 `--daemon`，dashboard.html 会在 `data/` 中自动生成。

#### 故障4：venv 激活报 "Command not found" 或 "python not recognized"

**现象**：`venv\Scripts\pip` 或 `venv\Scripts\activate` 执行失败。

**原因**：pyvenv.cfg 含旧 Python 路径（见 §3.6），或目标机器 Python 版本不匹配。

**解决**：删除 venv 重建（§六 命令），确保目标机器 Python 版本 ≥ 3.10。

#### 故障5：Windows 防火墙阻止 8790 端口

**现象**：dashboard.html 在本机浏览器可访问，但其他机器访问不了。

**解决**：Windows 防火墙 → 入站规则 → 新建规则 → 端口 → TCP 8790 → 允许。

#### 故障6：config.json 缺少字段时回退到 _DEFAULTS 旧路径

**现象**：config.json 只改了 `quant_dir`，没改 `legend` 字段，运行时 Legend 路径仍指向 C 盘/E 盘旧位置。

**原因**：`_deep_merge(_DEFAULTS, config.json)` —— config.json 没有的字段从 _DEFAULTS 取，_DEFAULTS 仍是旧路径。

**解决**：config.json **必须完整包含所有路径字段**，不能省略。或同时改 _DEFAULTS（§3.2）。

#### 故障7：长时间运行后 device.log 中的路径日志误导

**现象**：device.log 显示"量化目录未找到: C:\Users\Lenovo\Desktop\量化\..."，但实际 config.json 已改。

**原因**：这是 _DEFAULTS 的值，不是 config.json 的值。若 config.json 未完整修改，运行时确实用的旧路径。

**解决**：检查 `run.py --selftest` 输出，确认实际加载的路径。

### 12.4 安全删除C盘代码的最终检查清单

在删除C盘代码前，逐项确认：

- [ ] `pytest tests/ -q` → 119 passed ✓
- [ ] `run.py --selftest` → 16/16 ✓
- [ ] `run.py --once` → 四源数据正常采集 ✓
- [ ] `http://127.0.0.1:8790/dashboard.html` 可访问 ✓
- [ ] Legend 登录态正常（`--probe --legend` 在线）✓
- [ ] 量化项目路径正确（`quant_dir` 指向目标位置）✓
- [ ] `config.json` 所有字段已填写目标机器路径 ✓
- [ ] `device_config.py` `_DEFAULTS` 已同步修改 ✓
- [ ] C 盘无其他项目依赖本项目（检查快捷方式/计划任务）✓

### 12.5 C 盘删除前的备份建议

```bash
# 备份原始配置（万一改错可恢复）
cp config.json config.json.bak
cp device_config.py device_config.py.bak

# 导出核心文件哈希清单（转移后比对）
for f in run.py fusion.py ui_collector.py config.json device_config.py; do
  certutil -hashfile "$f" MD5
done > hashes_before_delete.txt
```

---

## 十三、一句话总结

**核心原则**：`config.json` + `device_config.py` 中的路径是"双保险"——**两处必须同时修改**，否则运行时会回退到旧路径。venv 必须删除重建（pyvenv.cfg 含旧路径无法修复）。其他文件直接复制即可。**C 盘删除前必须 100% 测试通过，不能再依赖任何旧路径。**
