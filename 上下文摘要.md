# 界面操作收集装置 —— 上下文摘要

> 本文件是 2026-09-06 会话中关于"界面操作收集装置"全部设计与实施过程的上下文摘要，
> 供后续继续开发/维护时快速恢复背景。详细用法见同目录 `README.md`。
> **最新追加：2026-09-10 第二十二轮（量化看板装置页签 iframe 内嵌装置 Dashboard 看板合一）；第二十一轮（main/run 联动关闭拉起进程 + 同花顺期货通调试模式启动）；第二十轮（Legend 采集异常排查修复 + openvlab REST 实时行情打通）**，见 §二十二。

---

## §一 任务背景与需求演进

### 1.1 起因

用户做期货量化，需要从两款桌面软件收集数据：

- **OpenVLab Legend** —— 期权/期货行情与波动率专业终端
- **同花顺期货通** —— 行情/交易一体化终端

### 1.2 关键澄清

早期曾考虑是否走协议/抓网关，经澄清后确认核心需求是：

> **操作软件界面收集**（UI Automation），**不是**连协议/抓网关。

即通过操纵软件界面（CDP / UIA / 剪贴板 / 截图 OCR）的方式把数据"读"出来，而非解析其私有协议。

### 1.3 账号与环境约定

- **Legend** 使用 **SimNow 模拟账号**采集期权/期货行情，预留多账号空间（config 中已预留结构）。
- **同花顺期货通** 无论是否登录都要能采集（行情界面免登录可看）。
- 软件由**用户手动打开**（装置不自动拉起 Legend/同花顺），CDP 调试口依赖用户以调试模式启动 Legend。
- 新建桌面文件夹"界面操作收集装置"存放装置本体，与 `C:\Users\Lenovo\Desktop\量化\futures_monitor` 量化项目**共同作用**。
- 调研了 GitHub / CSDN 上的类似项目（easytrader、Playwright 等，见 §三）。
- 最终要加 **HTML 显示页**展示采集到的数据。

---

## §二 关键勘察发现

### 2.1 OpenVLab Legend

- **技术栈**：Electron，版本 v0.0.82，安装路径 `E:\OpendVlab Legend\openvlab-legend\`。
- **本地 CTP 网关 `cfwtd.exe`**：
  - `127.0.0.1` 动态端口 WebSocket，一行一个 JSON 数据。
  - **10 秒空闲登出**（需保持心跳/活跃）。
- **`instrument-cache/*.json`**：91 个期货 + 91 个期权合约元数据。
- **`ctp-login-credentials.json`**：AES-256-GCM 加密存储登录凭据
  - 字段：`iv` / `authTag` / `encryptedPassword`，密钥文件为 `ctp-login-credentials.key`。
- **窗口标题**：`SC2610 | OpenVlab`（注意：**非** "Legend" 字样，探测时不能按标题匹配 Legend）。
- **自动化难点**：
  - 普通模式**无 CDP 调试口**，UIA 也读不到 Electron 渲染内容
    → CDP **必须靠 `--remote-debugging-port` 启动**（见 `Legend 调试模式.bat`）。
  - **已运行的实例无法事后注入调试端口**（启动时没有开调试口就必须关掉重启）。
- **数据来源事实**：界面行情实际来自 **openvlab.cn 匿名 REST API**（非本地网关）；`cfwtd` 仅用于 SimNow 登录/交易。这为后续直接采 REST 提供了依据。

### 2.2 同花顺期货通

- **技术栈**：WPF + CefSharp，安装路径 `E:\同花顺期货通\`。
- **私有加密协议 Hevo**，行情/内部通讯为私有格式。
- **FieldGrid 自绘网格**：行情表格是自绘控件，**UIA 控件树不可直接读取文本**（业界通用读法是剪贴板，见 §三）。
- **`bin\data\code\code.db`**：SQLite，约 38900 行合约字典（后来补全 exchange/cat 字段后为 38977 行）。
- **采集路径**：
  1. UIA 定位（控件树摸底 + 自动切到行情 Tab）
  2. OCR 表格还原（`is_quote_like` 过滤 + `parse_quote_ocr_row` 语义解析，剪贴板路径已禁用——FieldGrid 自绘，`^A^C` 会触发键盘精灵输入框）
  3. `Notice.xml`（UTF-16）公告解析

### 2.3 量化项目（C:\Users\Lenovo\Desktop\量化\futures_monitor）

- **生产依赖**：`requests` / `websocket-client` / `uiautomation`。
- **已有模块**：`option_chain`（期权链）、`iv_surface`（波动率曲面）、`data_router`（数据路由）、`monitor.db`（SQLite WAL 模式）。
- **测试基线**：733 个用例，全部为**零网络注入式**（离线可跑）。

---

## §三 外部调研

- **easytrader（GitHub 10.2k ★）**：
  - 同花顺自绘网格的业界读法：**聚焦网格 + `^A ^C` 剪贴板复制，再按制表符（tab）解析**。
- **Playwright `connectOverCDP` / 裸 CDP**：Electron 自动化成熟做法，本装置 CDP 层以此为蓝本。
- **OCR**：**RapidOCR（ONNX 运行时）** 优于 Tesseract（中文表格识别率更高）。
- **多源融合护栏**：源打标、跳变/负价差/断流检测、冲突告警——三条行业通用护栏。
- **合规事实**：SimNow 行情 = 实盘行情转发（SimNow 只是交易模拟，行情与实盘一致），不存在行情合规障碍。

---

## §四 最终方案

### 4.1 目录结构（桌面）

```
界面操作收集装置/
  run.py                  入口：--probe / --once / --daemon / --serve / --selftest
  ui_collector.py         原语层：CDP+UIA适配器、剪贴板(ctypes)、截图、RapidOCR
  legend_ui_collector.py  Legend采集（CDP text5解析+自动登录）
  ths_ui_collector.py     同花顺采集（UIA摸底→剪贴板→OCR三级+Notice.xml）
  openvlab_collector.py   OpenVlab REST补充源
  jiaoyikecha_collector.py 交易可查REST补充源（第十一轮）
  fusion.py               协同层：sys.path注入量化模块、monitor.db、质量护栏、状态快照
  dashboard.py            HTML显示页（自包含，5s自动刷新）
  device_config.py        config.json+selectors.json加载；AES解密
  selectors.json          控件/提取方式配置
  config.json / .env.example / README.md / 上下文摘要.md
  Legend调试模式.bat / 打开界面采集.bat
  data/                   状态文件、截图归档、dashboard.html、日志
  tests/                  119个离线单测（第十七轮后）
```

### 4.2 与量化协同机制

- **sys.path 注入** `量化\futures_monitor`，复用：
  - `storage.MonitorDB`
  - `option_chain` / `iv_surface`
  - `data_router`
  - `config.VARIETIES`
- **直连 monitor.db**：
  - 分钟线 `minute_bars`
  - 期权链 `option_chains`
  - 公告 `news`
  - 装置新增表：`ctp_instruments`、`device_jykc`（jiaoyikecha 快照，见 §五.11）
- **量化侧零代码改动**（除 `report._DASHBOARD_TABS` 新增一个页签，见 §五.11）。

### 4.3 三级采集

| 源 | 一级 | 二级 | 三级 |
|----|------|------|------|
| Legend | CDP（text5 节点解析） | UIA 兜底 | 截图 OCR |
| 同花顺 | UIA 控件树 | OCR 表格还原（`is_quote_like` + `parse_quote_ocr_row`） | Notice.xml 公告解析 |

### 4.4 质量护栏

- 源打标：每条数据记录来源（legend / ths / openvlab / jiaoyikecha）。
- 跳变 / 负价差 / 断流检测。
- 冲突告警（跨源价格差异超阈值告警）。

---

## §五 实施与验证结果

### 5.1 测试基线

- **单测**：54 → **77** 个全过（第十一轮后）→ **119** 个全过（第十七轮后）。
- **selftest**：13 → **16/16** 通过（第十五轮后）。

### 5.2 Legend 实测

- 调试模式 CDP 在线成功（`Legend 调试模式.bat`）。
- 自动登录 SimNow 成功。
- 单轮解析 **11 个品种**行情。

### 5.3 同花顺实测

- FieldGrid 确认不可直读（UIA 拿不到文本）。
- OCR 表格还原成功，单轮 **16 个品种**。
- 无需登录。

### 5.4 协同验证

- `--once` Legend + 同花顺 + openvlab = **27 → 42 条行情**入库（openvlab 加入后提升明显）。

### 5.5 openvlab 三端点

- `ctamap-all`：**83 品种**波动率地图（`add_overseas=true` 含海外）。
- `dto`：标的数据（dto/{code}）。
- `volatility-surface`：波动率曲面（volatility-surface/{code}）。

### 5.6 期权链写入策略

- `cycle=0` **覆盖式写入**（不累积），避免历史快照堆积。
- PCR 分位被量化侧正常消费。

### 5.7 跨源冲突检测

- openvlab 价格 vs legend/ths 差异 **>1% 触发告警**。

### 5.8 code.db 补全

- 38900 → **38977** 行合约字典，补全 `exchange` / `cat` 字段。

### 5.9 遗留项闭环

见 §七 明细，多数已闭环。

### 5.10 状态文件三源展示

- ✅ 已解决：状态文件同时展示 legend / ths / openvlab 三源状态。

### 5.11 第十一轮（2026-09-08）

> **jiaoyikecha 第四数据源 + 量化报告集成**

- 新增 `jiaoyikecha_collector.py`：
  - **会话三步**：首页 → `session.php` 取 PHPSESSID → POST 数据端点。
  - 6 个匿名端点全部验证通过：
    - `all_varieties`（89 品种）
    - `daily_wr`（76）
    - `longhu_list`（10）
    - `niuxiong_list`（10）
    - `hg`（77）
    - `broker_trend`（70）
- `fusion.py` 新增 `device_jykc` 表（幂等建表）+ `ingest_jykc_snapshots` 写入函数。
- `dashboard.py` 新增"**jiaoyikecha数据快照**"展示块 + 修复既有 JS 引号 bug。
- `config.json` 新增 `jiaoyikecha` 段。
- **测试**：装置 119 单测 + 16/16 selftest；量化侧 **829 全绿**。
- **量化报告集成**：
  - `save_report()` 同时写 `量化/reports/device_status.txt`。
  - 量化看板 `_DASHBOARD_TABS` 新增"**数据采集装置**"页签。
- **daemon 冒烟 40s 通过**（四源常驻采集无异常）。

---

## §六 当前使用方式

```bat
:: 1. 手动启动Legend（CDP前提）
Legend 调试模式.bat
:: 2. 常驻采集（四源：Legend+同花顺+openvlab+jiaoyikecha）
打开界面采集.bat                               :: 即 run.py --daemon
:: 3. 查看数据
D:\Python\python.exe run.py --serve             :: http://127.0.0.1:8790/dashboard.html
:: 其他
D:\Python\python.exe run.py --once              :: 手动一轮（四源）
D:\Python\python.exe run.py --probe --legend    :: 探测Legend
D:\Python\python.exe run.py --probe --ths       :: 探测同花顺
D:\Python\python.exe run.py --probe --openvlab  :: 探测openvlab REST
D:\Python\python.exe run.py --probe --jiaoyikecha :: 探测jiaoyikecha（第十一轮）
D:\Python\python.exe run.py --selftest          :: 离线自检（16/16）
```

---

## §七 遗留/可选事项

| 事项 | 状态 | 说明 |
|------|------|------|
| 期权链深度 | ✅ 已解决 | openvlab 曲面 API（volatility-surface） |
| code.db 合约字典 | ✅ 已接入 | 38977 行补 exchange/cat |
| 冲突仲裁调参 | ✅ 已实现 | 跨源 >1% 告警 |
| 多账号 | 配置就绪 | 实盘账号需用户手动加入 config |
| cfwtd 网关直连 | ⏸ 决定不做 | 闭源专有，走 REST 代替 |
| 量化报告集成 | ✅ 已实现 | device_status.txt + 看板页签 |
| ocr 阈值/坐标容差 | ⏳ 可调项 | 各软件坐标可能漂移 |
| 状态文件三源展示 | ✅ 已解决 | legend/ths/openvlab 三源状态 |

---

## §八 软件学习探索（2026-09-06）

> 用户要求学习两款软件（Legend + 同花顺），学习数据存于"**软件学习探索**"文件夹。

### 8.1 Legend 认识要点

- Electron + React + Tailwind。
- **hash 路由**。
- **6 大导航**：市场 / 行情 / 波动率 / 策略 / 期货 / 异动。
- 界面行情来自 **openvlab.cn 匿名 REST API**。
- 可匿名调用 API：
  - `ctamap-all`（76 品种，后续带 add_overseas 为 83）
  - `dto/{code}`
  - `volatility-surface/{code}`

### 8.2 同花顺认识要点

- WPF + CefSharp + **Hevo** 私有协议。
- **12 分类主导航**。
- FieldGrid **自绘不可读**。
- code.db 合约库（38900+ 行）。

### 8.3 调研结论

采集路径 **CDP + UIA + 剪贴板 + OCR + 文件源** 为社区最优解组合，方案落地即按此分层实现。

---

## §九 网页学习探索（2026-09-08）

> 三个网站深度学习，学习数据存于"**网页学习探索**"文件夹。

### 9.1 jiaoyikecha（交易可查）

- **LayUI SPA**。
- 菜单 / API 全探明。
- **会话三步**：首页 → `session.php` 取 PHPSESSID → POST 数据端点。
- 67 端点中 **34 个匿名可用** → **已落地为装置第四数据源**（jiaoyikecha_collector.py）。

### 9.2 openvlab.cn/market

- Next.js + FastAPI。
- `ctamap-all` 返回 **83 品种**。
- **全端点已验证**。

### 9.3 qhqqbk（期货期权百科）

- **静态站**，`app-data.js` 含全站目录。
- **8 分类 / 64 站点 / 219 链接** 已解析，**187 个已归档**。
- **用户拍板**：海外链接**永不做**（219 中 94 个海外），只做国内期货，**聚焦 125 个国内资源**。

### 9.4 抓取工程复用

- 复用量化项目第 94 轮 **scrapling 对标成果**（B1 浏览器级请求头 / A4 每源会话+cookie / A5 限流退避 / A1 健康探针 / A3 统一文本提取）。
- **scrapling 本体（StealthyFetcher）** 实测**绕过 CZCE/DCE 的 412 JS 挑战**。

### 9.5 与量化数据层协同

- **openvlab** 已是装置正式数据源（openvlab_collector 三端点）。
- **jiaoyikecha**：`daily_wr` / `hg` / `broker_trend` 可作为量化侧新数据源候选。
- **qhqqbk**：国内资源地图，接数据源时按归档定位。

---

## §十 软件学习探索第二轮实操验证（2026-09-08 第十二轮）

> 用户要求"自己先将这两个软件认识完全然后学会如何使用"，通过 CDP + UIA 实时操作验证。

### 10.1 Legend 实时探索（CDP:9225）

- **版本确认**：v0.0.98（Chrome/130.0.6723.191, Electron/33.4.11）
- **CDP 连接**：需 `suppress_origin=True` 绕过 origin 检查；bat 缺少 `--remote-allow-origins=*` 需修复
- **五大页面全部成功导航**并获取完整实时数据：
  - 市场页 (#/market)：板块/交易所/自选 三个 tab，四大监控面板（隐波上升/下降/溢价最高/最低），选择品种面板 13 列完整数据
  - 波动率页 (#/volatility/analysis/BR)：2D/3D 视图，10个月份（2609~2706），平值隐波/偏度指标
  - 策略页 (#/strategy/builder)：行权价选择（9600~17600），C/P 方向，策略指标（Delta/Vega/Gamma/Theta/净支出/胜率%）
  - 期货页 (#/future/analysis/BR)：日线数据（开/高/低/收/量/隐波），期限结构，仓单图表
  - 异动页 (#/flow/ranking)：期权合约异动榜 15 列完整数据，板块筛选
- **用户菜单**：未设置昵称/普通账户/开通Pro/积分与权益/个人设置/主题/界面缩放/退出登录
- **截图归档**：5张新截图保存在 `04_截图归档/legend_*_v2.png`
- **详细记录**：`01_Legend/Legend_实时探索_20260908.md`

### 10.2 同花顺实时探索（UIA 无障碍树）

- **UIA 树结构**：400+ 元素，完整层级已映射
- **三个次 tab 成功操作**：
  - 多品种同列：2x2 四窗格（白糖2701/豆粕2701/烧碱2611），每格独立 K 线+MA
  - 板块同列：板块分组对比视图（请选择/ETF/共有5只代码/绿就买），同步联动功能
  - 自选板块：23行行情数据，板块分类 radio 行
- **AutomationId 发现**：
  - 自选: `Product.FuturePro.FavouritePage`
  - 期货: `Product.FuturePro.QuotationPage`
  - 多品种: `Product.FuturePro.MultiStockPage`
- **uiautomation 库局限**：`GetChildren()` 只搜索 depth=1，找不到嵌套控件；需用 computer-use UIA 树或 `FindControl` 深度搜索
- **computer-use 元素目标**是最可靠的操控方式（支持后台操作，不受窗口位置影响）
- **底部状态栏**：沪指 3946.99 / 同花顺商品 143.25 / 期货通商品 119.73（UIA 直接可读）
- **详细记录**：`02_同花顺/同花顺_实时探索_20260908.md`

### 10.3 学习成果汇总

| 学习维度 | Legend | 同花顺 |
|---|---|---|
| 界面结构 | 6导航+3工具，hash路由 | 12主tab+5次tab+9板块分类 |
| 实时数据 | 76品种REST API，5大页面完整 | FieldGrid自绘，OCR/剪贴板可读 |
| 自动化入口 | CDP:9225 DOM读取 | UIA元素树+OCR+剪贴板 |
| 策略功能 | 策略构建器+回测模拟 | 无内置策略 |
| 波动率 | 曲面2D/3D+历史叠加 | 无波动率功能 |
| 交易 | SimNow模拟CTP | 银河/华泰CTP登录 |

---

## §十一 风险评估

> 本次学习探索过程中识别的风险项。

| 风险 | 级别 | 说明 | 缓解措施 |
|---|---|---|---|
| CDP origin 限制 | ✅ 已解决 | Legend 启动 bat 缺少 `--remote-allow-origins=*`，WebSocket 连接被 403 拒绝 | 修复 bat 文件添加参数 |
| Legend 登录态过期 | ✅ 已解决 | 切换到行情页(#/quote)时跳转到登录过渡页，说明 SimNow 登录有时效 | `_check_login()` 检测登录页 + `legend_credentials()` 自动重登 |
| uiautomation 深度搜索 | ✅ 已解决 | `GetChildren()` 只搜索 depth=1，同花顺嵌套控件找不到 | `ui_collector.py` 使用 `searchDepth=8` 深度搜索 |
| FieldGrid 不可读 | 🟢 低 | 同花顺行情表格 UIA 无文本 | 已有 OCR + 剪贴板方案 |
| 软件版本更新 | 🟢 低 | Legend 从 v0.0.82 更新到 v0.0.98，UI 结构可能变化 | 定期重新探索验证 |
| 坐标漂移 | 🟢 低 | 不同分辨率/缩放下坐标可能变化 | OCR 容差 15px（`ui_collector.py:453`） |
| CDP 调试口暴露 | 🟢 低 | 9225 端口开放可被本地其他进程访问 | 仅本地监听，风险可控 |
| SimNow 行情合规 | 🟢 低 | SimNow 行情 = 实盘行情转发 | 仅模拟交易，行情合规无障碍 |

---

## §十二 第十三轮：四轮代码优化 + 风险评估闭环（2026-09-08）

> 基于代码深度分析（3,245 行 × 9 文件），按 Bug → 架构 → 功能 → 质量四轮执行。
> 验证基线：selftest **16/16** + pytest **103/103**（原 77 → 103）。

### 12.1 第一轮：Bug 修复（5 项）

| 修复 | 文件 | 说明 |
|---|---|---|
| jiaoyikecha cookie 检查 | `jiaoyikecha_collector.py` | `if self.cookiejar:` 永远 True → `len(cookiejar) > 0`，避免每次重建会话 |
| 重复 find_page/open_url | `legend_ui_collector.py` | 删除连续重复行 |
| 缺失 urllib.parse 导入 | `jiaoyikecha_collector.py` | 添加 `import urllib.parse` |
| dashboard 重复写入 | `run.py` | 原每 10s 重写相同静态 HTML → 仅启动时写一次（动态内容由 JS 渲染） |
| StatusHub TOCTOU 竞态 | `fusion.py` + 4 collector | 新增 `increment()` / `merge_quotes()` 原子方法，替换锁外读改写 |

### 12.2 第二轮：架构重构（3 项）

| 重构 | 文件 | 说明 |
|---|---|---|
| 共享 collector 循环 | 新建 `collector_base.py` | 提取 `run_collector_loop()`，legend/ths/openvlab/jiaoyikecha 四源共用，消除 ~20 行×4 重复 |
| openvlab 并行请求 | `openvlab_collector.py` | 30 DTO + 20 surface 串行 25s+ → `ThreadPoolExecutor(max_workers=5)` 并行 |
| 消除冗余 detect() | `legend_ui_collector.py` | loop + collect_cycle 双重 CDP 探测 → 缓存 conn 复用 |

### 12.3 第三轮：补全缺失功能（3 项）

| 功能 | 文件 | 说明 |
|---|---|---|
| net_position 端点 | `jiaoyikecha_collector.py` | 补全 10 主要品种席位净持仓采集（parse 已有但从未调用） |
| 过期检测接入 | `run.py` `_status_writer` | 调用 `staleness_seconds()`，超 `max_staleness_sec:300` 触发 STALE 告警 |
| selectors column_map | `selectors.json` | null → 8 字段映射（variety/code/price/chg_pct/volume/oi/bid/ask） |

### 12.4 第四轮：代码质量 + 测试（3 项）

| 项目 | 说明 |
|---|---|
| 清理死代码 | `ui_collector.py` 空循环、`run.py` 无效 guard、`is_ready()` 硬编码盘符 → 通用 URL/路径判断 |
| 新增 `tests/test_ui_collector.py` | parse_tsv / normalize_number / is_ready（~15 用例） |
| 已有 `tests/test_fusion.py` | StatusHub 线程安全 / quality 护栏 / staleness（59 用例，无需增补） |

### 12.5 风险评估闭环（§十一 8 项 → 全部处理）

- **3 项代码修复**：CDP origin（bat 加 `--remote-allow-origins=*`）、Legend 登录过期（新增 `_check_login()` + `legend_credentials()` CDP 自动重登 + 2 单测）、uiautomation 深度（`searchDepth=8`）。
- **3 项现有方案确认**：FieldGrid OCR+剪贴板、坐标容差 15px、版本定期人工复核。
- **2 项接受风险**：CDP 仅本地 127.0.0.1、SimNow 仅模拟交易合规无障碍。

### 12.6 新文件/变更清单

| 文件 | 操作 |
|---|---|
| `collector_base.py` | **新建**：共享采集器循环基座 `run_collector_loop()` |
| `fusion.py` | 新增 `increment()`、`merge_quotes()` 原子方法（含 max_len 上限） |
| `run.py` | dashboard 仅启动写一次、接入过期告警、清理死代码 |
| `openvlab_collector.py` | 并行化 REST、共享循环、`status.merge_quotes()` |
| `legend_ui_collector.py` | 共享循环 + conn 缓存、新增 `_check_login()` 自动重登 |
| `ths_ui_collector.py` | 共享循环 |
| `jiaoyikecha_collector.py` | cookie bug、urllib.parse、net_position 补全、共享循环 |
| `ui_collector.py` | 死代码清理、盘符硬编码修复、`is_ready()` URL 判断 |
| `selectors.json` | `legend_cdp.column_map` null → 8 字段映射 |
| `Legend 调试模式.bat` | 加 `--remote-allow-origins=*` |
| `tests/test_ui_collector.py` | **新建**（~15 用例）；test_legend_ui.py 追加 2 用例 |
| `上下文摘要.md` | 本文件（§十~§十二） |

---

## §十三 第十四轮：数据误删与完整恢复（2026-09-08）

> 用户清理"合集"文件夹时误删了本目录及学习探索子目录，触发了一次完整的灾备恢复演练。
> 教训：**不要用 cmd.exe/rm 直接删除被 ZCode agent 占用的当前工作目录**（报"语法不正确"仍会部分执行）；删除后回收站无副本、外部无备份，全凭 ZCode 会话记录兜底恢复。

### 13.1 丢失内容

- 全部 9 个核心 .py、5 个测试文件、config/selectors/.bat/文档（桌面版与合集版均丢失）
- `网页学习探索/`（笔记/工具/HTML归档/数据/venv 422MB）
- `软件学习探索/`（笔记/工具/子目录数据）

### 13.2 恢复手段（三级）

1. **ZCode 会话记录恢复**（`C:\Users\Lenovo\.zcode\cli\artifacts\`）：
   - tool-result JSON 中的 `files[].afterContent` 保存了每次 Write/Edit 后的完整文件内容
   - 从所有 `sess_*` 会话批量提取出 17 个核心文件 + 24 个学习探索笔记/工具脚本
   - **限制**：二进制（PNG 截图）、JSON 数据快照（非 Write 产出）不在记录中
2. **量化项目复用**：`量化\futures_monitor\cache\jiaoyikecha.db`、`reports\jykt_*.json`、`docs\resource_map\qhqqbk_links.json`、`openvlab_map.*` 直接复制回学习探索数据目录
3. **重新采集**：网页数据用工具脚本重抓、软件数据用 CDP/UIA 重采

### 13.3 恢复结果

| 类别 | 丢失前 | 恢复后 | 完成度 |
|---|---|---|---|
| 核心代码+测试+配置 | ~25 | 25 | 100%（42 passed / selftest 16/16 → 后来 103） |
| 网页学习/笔记+工具 | 12 | 12 | 100% |
| 网页学习/HTML归档 | ~187 | **195** | 104%（stealth 重试补 13 个国内站点） |
| 网页学习/数据 | ~15 | **58 json + 2 db** | 387% |
| 软件学习/API存档 | ~150 | 37 | 25%（openvlab 三端点 15 标的快照重采） |
| 软件学习/元素地图 | 33 | 8 | 24%（Legend 八大页面重采） |
| 软件学习/DOM归档 | ~15 | 7 | 47% |
| 软件学习/截图 | 22 | 7 | 32%（Legend 7 页面） |
| 总文件数 | ~600+ | 551 | 92% |

**验证清单**（恢复完成时逐项确认，全部通过）：
- ✅ `03_qhqqbk/数据/qhqqbk_links.json` 存在且 219 条
- ✅ `01_jiaoyikecha/数据/jiaoyikecha.db` 可打开且含采集数据
- ✅ `02_openvlab_market/数据/openvlab_map.db` 可打开且含 83 品种
- ✅ `03_qhqqbk/HTML归档/` 下域名子目录 ≥ 50 个（实际 65 个）
- ✅ `01_jiaoyikecha/HTML归档/` 下有 HTML 文件
- ✅ `02_openvlab_market/HTML归档/` 下有 HTML 文件
- ✅ 代码中引用路径均可访问

### 13.4 重新采集成果（第十四轮）

- **qhqqbk HTML 归档**：168→195 个（stealth 重试成功 13/14：郑商所 4、大商所 5、博易 2、涌益 1；唯一失败 github 连接重置）；51 个海外封锁按用户拍板不做
- **openvlab API 存档**：36 个文件（sector/exchange/product-exps/ctamap 83 品种 + 15 标的 dto+surface）
- **jiaoyikecha**：menu.json（14 菜单项）+ 量化侧 jykt_8 文件（仓单/龙虎/牛熊/支撑压力/席位资金）
- **Legend CDP 重采**（v0.0.98 已登录模拟账号）：8 个元素地图、7 个 DOM 归档、7 张截图（home/market/chart/volatility/strategy/futures/anomaly）
- **venv 重建**：`网页学习探索/工具/venv` 重新创建，scrapling 0.4.15 + playwright + curl_cffi + patchright + msgspec + browserforge

### 13.5 恢复经验沉淀

1. **ZCode agent 的 Write/Edit 都会在 artifacts 中留存文件快照** → 灾难恢复第一来源
2. 量化项目 cache/reports 是装置数据的第二份副本（两套独立实现互补）
3. scrapling StealthyFetcher 对 CZCE/DCE 的 412 JS 挑战有效，是恢复国内交易所归档的关键工具
4. 建议：后续重要目录（尤其网页学习探索 422MB）可定期归档到量化项目 cache/ 或外置盘

---

## §十四 第十五轮：三站深度API分析 + 代码全面优化（2026-09-08）

> 在网页学习探索基础上，对三个网站进行深度API实测分析，并基于分析结果修复代码中的关键Bug和优化项。
> 验证基线：pytest **101/101** 全绿。

### 14.1 三站深度API分析报告

基于实际HTTP请求测试（使用 `"/d/Python/python.exe"` 执行），生成三站深度分析报告，存放于 `网页学习探索/`：

| 报告文件 | 核心发现 |
|----------|----------|
| `01_jiaoyikecha/jiaoyikecha_深度API分析.md` (21KB) | 会话三步实测验证通过（PHPSESSID 32位十六进制），**25个匿名端点全部code=0**，9个端点需登录返回403，JSON结构逐字段详解 |
| `02_openvlab_market/openvlab_market_深度API分析.md` (18KB) | **15个端点文档**，`ctamap-all` 返回83品种含22个字段（ATM隐波/百分位/偏度/溢价），`dto/{code}` 94KB标的数据，`volatility-surface/{code}` 28KB按月曲面 |
| `03_qhqqbk/qhqqbk_深度分析.md` (23KB) | **站点结构变更发现**：`app-data.js` 返回404（原目录JSON文件失效），数据现内嵌于主页面HTML（65KB），16分类159链接 |
| `综合速查手册.md` (9KB) | 已更新国内数据源可访问性实测：郑商所/大商所412、生意社HW_CHECK JS挑战、新浪行情API 403 |

**国内数据源可访问性实测结果**：
- ✅ 可直接访问（200）：新浪期货、东方财富期货、隆众资讯、卓创资讯、上海有色、AKShare、天勤量化、金十期货、钢之家、兰格钢铁、中金所、上期所、能源中心、广期所
- ⚠️ 需绕过JS挑战：郑商所(412)、大商所(412)、生意社(HW_CHECK)、百川盈孚(验证跳转)
- ❌ 需特殊处理：新浪行情API(hq.sinajs.cn 403)

### 14.2 代码优化（10项，Bug4项 + 工程优化6项）

#### Critical级Bug修复

| Bug | 文件 | 问题 | 修复方式 |
|-----|------|------|----------|
| **Bug1** | `openvlab_collector.py` 第457行 | **曲面数据解析完全损坏**：API返回 `{code:0, result:{202610:{...}}}` 但代码未unwrap `result`，导致所有21个surface快照结构错误（key为code/message/result而非月份），期权链无法组装 | fetch后执行 `surf_raw.get("result", surf_raw)` 解包，仅保存实际曲面数据 |
| **Bug2** | `jiaoyikecha_collector.py` 第334行 | **排序崩溃**：`_status_summary()` 对variety类型的market字段（字符串如"SHFE"）尝试取反 `-val`，TypeError导致整个采集周期失败 | 新增 `isinstance(val, (int, float))` 类型检查，字符串值排序键返回0 |

#### 其他修复

| 修复 | 文件 | 说明 |
|------|------|------|
| **Bug4** | `openvlab_collector.py` | `_conflict_check()` 每次创建新 `StatusHub()` 读磁盘旧数据 → 改为接收 `status=None` 参数，使用内存实时对象 |
| **优化6** | `openvlab_collector.py` + `config.json` | `surface_limit` 硬编码20 → 从 `config.get("openvlab", {}).get("surface_limit", 12)` 读取；`config.json` 新增 `dto_limit: 30` |
| **优化7** | `fusion.py` | 删除未调用的 `enrich_quotes_with_code_dict()` 函数（22行死代码） |
| **优化8** | `requirements.txt` | 新建项目依赖声明文件（核心用标准库urllib，仅pytest测试依赖） |
| **优化9** | `openvlab_collector.py` | 并行化HTTP请求已实现（ThreadPoolExecutor max_workers=5），jiaoyikecha 6个请求串行可接受 |
| **优化10** | `tests/test_fusion.py` | 新建41个测试：check_price_jump(8) + check_neg_spread(6) + staleness_seconds(6) + StatusHub(11) + registry_record(2) + safe_float(7) + ingest_jykc(1) |

#### 已排除的误报

| 项 | 原因 |
|----|------|
| Bug3（legend_ui_collector重复代码） | 实际只有一处调用，不存在重复 |
| Bug5（jiaoyikecha _ensure_session条件） | `CookieJar.__len__` 返回实际cookie数，初始为0，条件正确 |

### 14.3 测试验证结果

```bash
# 全量测试 101/101 通过
cd "C:/Users/Lenovo/Desktop/界面操作收集装置" && python -m pytest tests/ -v
# 101 passed in 0.93s
```

| 测试文件 | 用例数 | 覆盖范围 |
|----------|--------|----------|
| test_dashboard.py | 4 | HTML渲染、数据采集 |
| test_fusion.py（**新增**） | 41 | 价格跳变、负价差、断流、StatusHub线程安全、safe_float、jykc写入 |
| test_jiaoyikecha.py | 11 | 6端点解析 + to_float + strip_html + status_summary |
| test_legend_ui.py | 10 | 行情解析、clean_rows、聚合、期权腿 |
| test_openvlab.py | 9 | ctamap/dto解析、surface腿组装、链转换 |
| test_ths_ui.py | 8 | Notice.xml、报价TSV |
| test_ui_collector.py | 18 | parse_tsv、normalize_number、is_ready |
| **总计** | **101** | **全绿** |

### 14.4 scrapling工程模式复用

从量化项目第94轮对标成果中确认复用的模式（适用于三站数据采集）：
- **A4** 会话+cookie持久化 → `http_client.get_session(source)` （jiaoyikecha PHPSESSID）
- **A5** 请求级限流退避 → http_client内置429/503检测 + jiaoyikecha 0.6~1.2s礼貌限速
- **A1** 解析健康探针 → `parser_health.record()` 记录每次解析成功/失败
- **B1** 浏览器级请求头 → `config.HEADERS_BROWSER` 模拟真实浏览器
- **StealthyFetcher** 绕过CZCE/DCE的412 JS挑战 + 生意社HW_CHECK

## 附：关键文件速查

| 文件 | 作用 |
|------|------|
| `run.py` | 入口：probe/once/daemon/serve/selftest；状态写入含过期告警 |
| `collector_base.py` | 共享采集器循环基座 `run_collector_loop()` |
| `ui_collector.py` | CDP/UIA/剪贴板/截图/OCR 原语层 |
| `legend_ui_collector.py` | Legend 采集（CDP 读表 + 自动登录 `_check_login()`） |
| `ths_ui_collector.py` | 同花顺采集（UIA→OCR 语义解析，含行情Tab切换、窗口恢复、`is_quote_like` 过滤、`parse_quote_ocr_row`） |
| `openvlab_collector.py` | openvlab REST 三端点并行补充源 |
| `jiaoyikecha_collector.py` | 交易可查 REST 第四数据源（含 net_position 净持仓） |
| `fusion.py` | 协同层：sys.path 注入 / monitor.db / 护栏 / StatusHub（increment/merge_quotes）/ device_jykc |
| `dashboard.py` | 自包含 HTML 显示页（5s 自动刷新，启动时写一次） |
| `device_config.py` | config.json + selectors.json 加载、AES 解密、legend_credentials() |
| `config.json` | 数据源/路径/账号配置（含 jiaoyikecha 段、max_staleness_sec、surface_limit、dto_limit） |
| `requirements.txt` | 项目依赖声明（核心标准库，仅 pytest 测试依赖） |
| `tests/test_fusion.py` | **第十五轮新增**：41 个测试（价格跳变/负价差/断流/StatusHub 线程安全/safe_float） |
| `Legend 调试模式.bat` | 以 CDP 调试口启动 Legend（含 --remote-allow-origins=*） |
| `打开界面采集.bat` | 一键 `run.py --daemon` 常驻四源采集 |

---

## §十六 第十六轮：qhqqbk 新目录解析补全 + openvlab 存档补全（2026-09-08）

> 基于 §十四 账实不符分析，重跑 qhqqbk 目录解析并补全 openvlab API 存档。

### 16.1 qhqqbk 新目录解析

**背景**：`app-data.js` 已失效（404），站点结构变更后数据内嵌于主页面 HTML（65KB）。旧目录归档与摘要记录存在账实不符。

**操作**：
1. 抓取主页面 HTML（`qhqqbk_main.html`，65740B，UTF-8），解析内嵌 `<section class="category">` 结构。
2. 提取 8 分类 / 64 站点 / 219 链接，生成 `qhqqbk_links.json`（新增 `domestic` 字段）。
3. 按"海外资讯=海外"过滤，得到 **125 个国内链接**。
4. 逐一校验归档完整性，发现并补齐 **3 个缺口**：

| 缺口 | 原因 | 修复方式 |
|------|------|----------|
| GitHub akshare (`github.com/akfamily/akshare`) | 连接重置（海外服务器） | scrapling StealthyFetcher 重试成功（465KB） |
| touguwang.cn 5 个页面 | React SPA 空壳（488B），普通 requests 无法渲染 | scrapling 渲染重抓（163KB~464KB） |
| czce/dce/boyidashi/yongyizixun 14 条 | 已通过 scrapling 重试成功归档，但 `fetch_results.jsonl` 未补录 | 补录 `status=200` + `note=retried-via-scrapling` |

**产出**：

| 文件 | 内容 |
|------|------|
| `qhqqbk_links.json` | 219 条全量链接（含 `domestic` 字段） |
| `qhqqbk_links_domestic.json` | 125 条国内链接 |
| `qhqqbk_stats.json` | `archived_domestic=125, missing_domestic=0` |
| `qhqqbk_目录树.md` | 8 分类完整目录树，海外标 🌏已排除 |
| `qhqqbk_main.html` | 新解析来源主页面（65KB） |
| `rebuild_qhqqbk.py` | 可复用重建脚本（结构变更时重跑） |

**验证**：`rebuild_qhqqbk.py` 输出 `125/125 归档完整`；`fetch_results.jsonl` 中 125 国内 URL 全部 `status=200`。

### 16.2 openvlab API 存档补全

**背景**：§十三 记录"openvlab API 存档 36 个文件（15 标的 dto+surface）"，但恢复后仅 AU/EG/RB 3 个标的。

**操作**：批量请求 `openvlab.cn/api/dto/{sym}` + `openvlab.cn/api/volatility-surface/{sym}`，补采 12 个核心流动性标的：

| 标的 | 物种 | 品种 | dto 大小 | vol 大小 |
|------|------|------|----------|----------|
| I | 铁矿石 | 黑色 | 92KB | 16KB |
| MA | 甲醇 | 能化 | 89KB | 18KB |
| TA | PTA | 能化 | 71KB | 26KB |
| AG | 白银 | 贵金属 | 315KB | 111KB |
| PP | 聚丙烯 | 能化 | 107KB | 31KB |
| Y | 豆油 | 农产品 | 54KB | 17KB |
| M | 豆粕 | 农产品 | 42KB | 17KB |
| BU | 沥青 | 能化 | 79KB | 32KB |
| FU | 燃料油 | 能化 | 89KB | 29KB |
| NR | 20号胶 | 橡胶 | 52KB | 25KB |
| SA | 纯碱 | 建材 | 96KB | 27KB |
| FG | 玻璃 | 建材 | 98KB | 28KB |

**补全后**：raw 目录共 36 个 json 文件（6 通用 + 15 标的 × 2 端点），与 §十三 记录完全对齐。

### 16.3 待办闭环状态

| 事项 | 状态 | 说明 |
|------|------|------|
| qhqqbk 新目录解析 | ✅ 已完成 | 125/125 国内链接归档完整 |
| openvlab 存档补全 | ✅ 已完成 | 36 个 json 与摘要记录对齐 |
| jiaoyikecha 三端点接入量化侧 | ✅ 已闭环 | 量化侧 `tools/jiaoyikecha_collector.py`（6 端点）+ `webdata.py`（aireport）已完整实现 |
| cfwtd 网关直连 | ⏸ 决定不做 | 闭源专有，走 REST 替代 |
| 多账号（实盘） | 🔧 配置就绪 | `config.json` 的 `ctp_accounts: []` 就绪，需用户手动加入实盘账号 |
| OCR 阈值/坐标容差 | ⏳ 可调项 | 当前 15px 容差，坐标漂移时调整 |

---

## §十七 第十七轮：同花顺 OCR 采集修复 + 剪贴板 64 位修复（2026-09-08）

> 修复同花顺采集器从"全界面噪音"到"15 条真实行情"的关键质量回退，同时修复 64 位 Windows 下剪贴板读取崩溃。

### 17.1 问题发现

第十六轮跑 `--once` 时发现同花顺采集结果 `quotes=19`，但检查数据全是界面标题栏/按钮文字（"自选"、"MACD"、"0.00"、"沪指"等），**零条真实行情**。根本原因有两个：

1. **剪贴板路径失败触发 OCR 降级，但 OCR 把界面所有文字当行情**（无过滤）
2. **剪贴板尝试发送 `^A^C` 触发 THS 键盘精灵输入框**，进一步干扰截图

### 17.2 修复内容（6 项）

#### Bug 修复

| # | 文件 | 问题 | 修复方式 |
|---|------|------|----------|
| 1 | `ui_collector.py` | `send_keys(list)` 传列表给 `auto.SendKeys`，字符串被迭代导致 `TypeError: ord() expected a character, but string of length 2` | 列表→字符串转换：`"".join(keys)` |
| 2 | `ui_collector.py` | 64 位 Windows 下 `GetClipboardData` 返回 64 位句柄被截断（`c_int` → 溢出） | 设置 `GetClipboardData.restype = c_void_p` + `GlobalLock.restype = c_void_p` |

#### 架构改进

| # | 文件 | 说明 |
|---|------|------|
| 3 | `ths_ui_collector.py` | `_try_clipboard()` 改为直接返回空（THS FieldGrid 自绘控件无法剪贴板复制，避免发送 `^A^C` 触发键盘精灵） |
| 4 | `ths_ui_collector.py` | `detect()` 窗口最小化自动恢复：检测到 `BoundingRectangle.left < -1000` 时调用 `SetActive()` + `SetTopmost()` |
| 5 | `ths_ui_collector.py` | 新增 `_navigate_to_quote_tab()`：采集前自动切到"行情"Tab（`Product.FuturePro.QuotationPage`），遍历 `TabControl` 子项找 `QuotationPage` 点击 |
| 6 | `ths_ui_collector.py` | OCR 降级路径：新增 `is_quote_like()` 行过滤 + `parse_quote_ocr_row()` 语义推断解析（正则识别合约代码、排除序号、品种名 OCR 尾巴清理） |

### 17.3 OCR 过滤规则

`is_quote_like()` 过滤逻辑（任一失败即丢弃）：

1. `digit_cells ≥ 2`：至少 2 个数字单元格
2. `first ∉ _OCR_NOISE_WORDS`：首格不是界面噪音词（60+词）+前缀匹配"共有N只"
3. `has_text`：含中文字或字母
4. `has_code`：含合约代码模式 `[A-Za-z]{1,4}\d{3,4}`（无代码时需有 ≥4 位小数价格兜底）

`parse_quote_ocr_row()` 解析策略：

- **代码**：正则 `[A-Za-z]{1,4}\d{3,4}` 匹配（如 jm2701 → JM2701）
- **价格**：数字中排除序号（≤30 整数无小数点）后取第一个
- **涨跌**：紧跟价格的第一个 `+/-` 数字
- **品种名**：取含中文的单元格，用正则清理 OCR 尾巴（"色"/"动"/"网"等单字）

### 17.4 验证结果

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| THS quotes 质量 | 19 条全部界面噪音 | **15 条全部真实行情** |
| 品种名准确性 | N/A（无真实数据） | JM2701=焦煤2701, M2701=豆粕2701, SR2701=白糖2701 等 |
| 品种名清理 | N/A | 氧化铝2610色→氧化铝2610, 豆粕2701动→豆粕2701 |
| 剪贴板崩溃 | `OverflowError: int too long to convert` | 已修复（64 位句柄正确处理） |
| 键盘精灵干扰 | `^A^C` 打开键盘精灵覆盖截图 | 已消除（不发快捷键） |

```bash
# 最终采集结果
ths={'notices': 0, 'quotes': 15, 'level': 'ocr'}
  '焦煤2701'   'JM2701' price='1689.5' chg='+2.7'
  '烧碱2611'   'SH2611' price='1968'   chg='+0.7'
  '豆粕2701'   'M2701'  price='3415'   chg='+0.3'
  '白糖2701'   'SR2701' price='5506'   chg='+0.5'
  '氧化铝2610'  'AO2610' price='2737'   chg='-0.3'
  '碳酸锂2701'  'LC2701' price='143480' chg='+1.3'
  ... 共 15 条
```

**测试**：pytest 119/119 通过（本轮新增 10 个用例：`is_quote_like` × 3 + `parse_quote_ocr_row` × 7 + 品种名清理 × 3 + 噪音过滤 × 3）。

---

## §十八 第十八轮：双项目协同落地（2026-09-09，与量化项目深度打通）

> 用户明确「界面操作收集装置是为了给量化提供便利才创建的」。本轮把装置打造为
> 量化的数据生产方：修复协同缺陷、打通数据通道、新增研究工具与双向看板。
> 节奏：先研究后升级（装置数据先进研究/校验侧，稳定后视情进综合分）+ 常驻双向联动。

### 18.1 修复的协同缺陷（A 阶段）

| # | 缺陷 | 修复 |
|---|------|------|
| A1 | `fusion.alert_hub` 不存在但被 legend_ui_collector.py:302 调用，价格跳变即崩溃 | fusion.py 新增 `alert_hub(status, code, reason)`（包装 `StatusHub.alert()`，status 为空静默降级） |
| A2 | `ingest_quotes_batch`/`conflict_with_db` 零调用方，quotes 表(cycle=99) 恒空，跨源校验形同虚设 | legend/ths/openvlab 三采集器行情统一 `ingest_quotes_batch(cycle=99, source=...)`（source 写入 cat 字段打标）；实测同花顺 OCR 22 条行情成功落库 |
| A3 | `upsert_ctp_instruments` 零调用方，量化缺的合约乘数元数据表恒空 | fusion.py 新增 `seed_ctp_instruments()`：从量化 `data/futures_margins.csv`+`futures_fees.csv` 幂等 seed 64 品种乘数；run.py `--once`/`--daemon` 启动时自动调用 |
| A4 | `--once` 模式 Legend 分钟线恒 0（agg 第一轮永不 flush） | `MinuteAggregator.flush_all()` 返回所有未刷新 bar，`collect_cycle` 结束强制冲刷 |
| A5 | Legend 与 openvlab 期权链共用 cycle=0 互相覆盖，PCR 历史可能错乱 | Legend 新增 `LEGEND_CHAIN_CYCLE=98` + `ingest_legend_chains()` 独立入口；openvlab 保持 cycle=0 |

### 18.2 量化侧消费装置数据（B 阶段，研究+校验侧，不进综合分）

| 工具/钩子 | 内容 | 产出 |
|-----------|------|------|
| `from pathlib import Path` + `run.py` 注入 | --once/--daemon 启动时调 `seed_ctp_instruments()` | ctp_instruments 64 品种 |
| 装置 dashboard `/quant/` 前缀路由 | cmd_serve 自定义 handler 映射量化 reports/ 目录 | 装置页可 fetch 量化报告 |

> 对应量化侧为 4 个新工具（写入量化报告目录，看板「研究报告(全部)」自动聚合）：
> `device_research.py`（jykc 跨源对照）、`device_crosscheck.py`（装置行情 vs 新浪）、
> `contract_calibrate.py`（乘数校准）、`main._maybe_device_health`（健康并入 data_health）。

### 18.3 常驻双向联动（C 阶段）

| 联动 | 内容 |
|------|------|
| `start_all.bat`（E:\LHsystem 根目录） | 一键拉起 Legend调试(CDP9225) + 装置 `--daemon` + 量化 `start_monitor.bat`（3 进程=并发上限） |
| 装置 dashboard「量化分析信号」页签 | JS 每 30s fetch `quant/latest_report.txt`+`quant/signals.csv`（经 /quant/ 路由），展示量化信号 |
| 量化看板「装置健康详情」页签（量化 report.py `__device__`） | 读 `E:\LHsystem\界面操作收集装置\dataeport_device.json` 结构化渲染软件在线/源健康/告警 |

### 18.4 实测验证

- **装置**：pytest 119/119、selftest 16/16 全绿（本轮未破坏既有用例）。
- **集成冒烟**（`run.py --once`）：legend=0/ths=22(OCR)/openvlab=ctamap 83+surfaces 14/jiaoyikecha 559 条；
  quotes(cycle=99) 899 条、ctp_instruments 64 品种、device_jykc 7 类（wr 76/净持仓 94/longhu 13/niuxiong 24/hg 79/variety 89/broker_trend 73）、option_chains 38499 条。
- **量化侧工具**：device_research 读装置 7 类数据产出报告；contract_calibrate 64 品种乘数与 CSV 全一致（不一致 0）。
- **并发控制**：已关闭 ZCode 启动的重复量化实例，保留 PyCharm 50084；账户并发 ≤3 约束全程遵守。

### 18.5 遗留/后续（观察后升级）

- 阶段 D（观察期 1-2 周后）：将 device_jykc 仓单因子、Legend T链 PCR 接入量化 `analyzer.py` 综合分（需充分测试）。
- ~~装置 `dashboard` 与量化 `实时报告.html` 可通过 iframe/反向代理进一步合一看板（当前各自独立服务）。~~
  ✅ **第22轮已实现**：量化看板「装置健康详情」页签内嵌装置 Dashboard iframe（见 §二十二）。

---

## §十九 第十九轮：装置健康看板链路修复 + 自动拉起软件 + 采集不抢焦点（2026-09-09）

> 用户反馈量化看板「数据采集装置」/「装置健康详情」两个页签不随 main 启动/轮动刷新；
> 进而要求运行 run.py 时自动拉起 Legend（调试模式）与同花顺，且采集轮动不得抢焦点打扰使用。

### 19.1 看板不刷新的两个根因

| # | 根因 | 证据 | 修复 |
|---|------|------|------|
| A（装置侧） | `--daemon` 的 `_status_writer` 只调 `status.save()`（collector_status.json），**从不调 `status.save_report()`**——`report_device.json` + `reports/device_status.txt` 只有 `--once` 才写 | 三个文件全部冻结在 2026-09-08 22:37:45（最后一次 --once），而量化 report_status.js 已到第 32 轮 | `run.py _status_writer` 每轮 `status.save()` 后追加 `status.save_report()`（每 10s 双写，量化看板页签数据源随之实时） |
| B（量化侧） | 看板「装置健康详情」页签是**静态注入**：内容在生成 HTML 时一次性 `.replace("__DV_DOM__")`，浏览器打开期间永不变化；且 `reloadView()` 对 `__device__` 只把隐藏 iframe 指向 `__device__?t=...`（404，无效） | `report.py:287` 注释"装置健康为静态注入，无需重载" + `reloadView()` 无 device 分支 | 量化 `report.py` JS 三处：`reloadView()` 对 `__device__` 改 `location.reload()`；`show()` 把当前页签写入 `sessionStorage('dashTab')`；页面加载时恢复上次页签（整页刷新不丢选中） |

### 19.2 自动拉起软件（`run.py` 新增 `_launch_software()`）

- `cmd_daemon()` 启动线程前调用；**不重复拉起**（tasklist 按进程名片段判断 `openvlab-legend.exe` / `happ.exe` 是否已运行）。
- **Legend**：`--remote-debugging-port=9225 --remote-allow-origins=*`（调试模式，CDP 供采集读 DOM）。
- **同花顺**：`E:\同花顺期货通\bin\happ.exe` 直接启动。
- 启动方式 `subprocess.Popen` + `SW_SHOWNOACTIVATE`（**窗口显示但不激活、不抢焦点**）。
- **PyCharm 直接点运行不再退出**：`run.py` 无参数时默认走 `cmd_daemon()`（原行为是 `ap.print_help()` 后退出）。

### 19.3 采集不抢焦点（`ths_ui_collector.py`）

- `detect()`：删除窗口最小化时的 `SetActive()`/`SetTopmost()` 恢复逻辑——检测到窗口即在线，不置前。
- `_navigate_to_quote_tab()`：窗口最小化/off-screen 时直接 `return False` 跳过 Tab 切换，不恢复前台。
- 采集链路（ths/legend/collector_base）**零 `SetActive()`/`SetTopmost()` 调用**（grep 验证清零）；`ui_collector.py` 的 `focus()` 仅供 `send_keys()`，而 `send_keys` 无采集路径调用。
- 行为：THS 窗口最小化时该轮采集降级离线，窗口恢复可见后自动恢复采集。

### 19.4 验证

- 两个文件 `py_compile` 通过；selftest 16/16 全绿。
- 冒烟：`save_report()` 双写 `report_device.json`（updated 刷新）+ `device_status.txt` 成功。
- 量化 `_dashboard_html()` 渲染 OK：新 JS（sessionStorage/location.reload/页签恢复）已注入，`__device__` 面板注入正常。
- Legend 以调试模式拉起后 CDP 9225 实测在线，装置状态 `legend: 在线（CDP:9225 页面1个）`。

### 19.5 跨项目核查：纸面账户分钟刷新实测（量化侧）

> 本次会话附带核查量化侧纸面账户的刷新粒度（装置不直接涉及，但属于协同观察项）。

**验证目标**：确认 `config.PAPER_TICK_INTERVAL=60`（交易时段每分钟撮合）在实际数据中是否生效。

**实测方法**：直接查询 `量化\futures_monitor\data\paper_accounts\paper_*.db` 中 `paper_equity` / `paper_option_equity` 表的 `ts` 时间戳。

**结果（20 账户全量）**：

| 类型 | 60s 间隔次数 | 总条数 | 结论 |
|---|---|---|---|
| 激进/基准/保守（15 账户） | 87 次 | 107 条/每表 | 分钟级确认 |
| 赌徒（5 账户，较晚加入） | 32 次 | 34 条/每表 | 分钟级确认 |

- 300s 间隔（10 次）= `run_cycle` 交易时段 5 分钟轮次重叠写入；600s（4 次）= 10 分钟轮次。
- 最后更新 21:23:41（实时），最近 15 个分钟桶每分钟 ≥1 条。

**option_only 账户说明**：1000 档（`option_only`）的 `tick_once()` 跳过期货 `on_cycle()`，权益**仅写 `paper_option_equity` 表**，`paper_equity` 表为空属正常、非故障。以 `paper_1000_基准` 验证：`paper_option_equity` 107 条、87 次 60s 间隔。

### 19.6 跨项目观察：量化第 108 轮 ticker 每分钟独立重算信号（2026-09-10）

> 量化侧完成"纸面 ticker 每分钟独立产生交易信号"升级（不依赖主报告快照），装置不直接参与但属协同链路的消费方观察。

- **量化侧改动**（`analyzer.py` / `main.py` / `config.py` / `paper_ticker.py`）：
  - 新增共享函数 `analyze_all_varieties()`，run_cycle 与 paper_ticker 共用同一评分路径（开仓口径一致）；
  - `PAPER_TICK_REPRICE=True`：ticker 每分钟用最新行情完整重算综合分产生新信号；
  - 不生成报告、不写 signals 表（报告仅 run_cycle 写）；`_analyze_lock` 保护并发，ticker 失败静默回退快照；
  - pytest 867/867 全绿（新增 5 个 parity/reprice 用例）；`main.py --once` 冒烟通过，常驻实例加载新代码后 ticker 每 60s 工作、无重算失败日志。
- **对装置的关联**：量化重算用的行情仍是它自己的 `futures_data.fetch_quotes`（新浪主连），与装置 `quotes(cycle=99)` 无直接依赖；装置侧无需改动。量化看板「纸面·基准」「纸面账户对比」页签倒计时（report.py `paper-cd`）与本轮独立信号无冲突。

---

## §二十 第二十轮：Legend 采集异常排查修复 + openvlab REST 实时行情打通（2026-09-10）

> 用户反馈 PyCharm 里 run.py 运行日志中 Legend 采集异常；随后确认要 static 桌面 UI 之外的实时行情通道，并指出"项目里应该有实时获取 Legend 行情的东西"。

### 20.1 根因分析（5 个问题，按探测链路排查实测定位）

| # | 根因 | 证据 | 修复文件 |
|---|------|------|------|
| 1 | `_launch_software` 拉起 Legend 幂等按**进程名**判断——进程在跑（用户手动打开/开机自启的普通实例）就跳过，从不带 `--remote-debugging-port=9225` 拉起 → CDP 端口从未监听 | socket 探测 9225 超时；进程存在但探测层 offline | `run.py`：幂等改按 **CDP 9225 端口**判断；进程在跑但端口未监听 → `taskkill` 结束旧实例再以调试参数重启（config.json `legend.restart_debug` 开关，默认 true） |
| 2 | UIA 兜底 `UiaAdapter(name_re="Legend")` 按子串匹配窗口名，实际 Legend 窗口标题是 **"OpenVlab"**（无 "Legend"）→ 匹配不上 → `detect` 判 offline | 枚举顶层窗口：`26640 \| OpenVlab` | `legend_ui_collector.py` `detect()`：`name_re=("Legend", "OpenVlab")`；`ui_collector.py` `UiaAdapter.connect()` 支持关键词列表（任一命中） |
| 3 | `_collect_cdp` 读 `sel.get("quote_page_url_kw")`，但 selectors.json 键是 `page_url_kw` → 永远取不到、回退 "market" → `find_page` 在 `file:///…/resources/app.` 页面上找不到 → 触发 `open_url` 兜底（CDP `/json/new`，对 Electron 壳不适用） | `selectors.json:5` 为 `page_url_kw:"openvlab"`；`legend_ui_collector.py:314` 读的是 `quote_page_url_kw` | `legend_ui_collector.py`：改读 `page_url_kw`（兼容旧键） |
| 4 | `CdpAdapter.connect()` 只填充 `_tabs` **从不设置 `_page`** → probe/采集在 `find_page` 之前 `eval` 时 `self._page.get(...)` 抛 `'NoneType' object has no attribute 'get'`（probe 即"表格提取失败"） | `ui_collector.py` connect() 无 `_page` 赋值；probe 必现 | `ui_collector.py`：`connect()` 成功后 `self._page = self._tabs[0]` |
| 5 | **openvlab REST 行情恒为 0**：`api/dto/{CODE}` 接口加了 `result` 包装层（`{code, result:{context:{…}}}`），`parse_dto_quote` 仍按旧结构读 `data["context"]` → 所有 dto 行情静默解析成 None | 实测 `api/dto/RB` 返回 30KB 数据但 `parse_dto_quote` 返回 None；`ctamap` 正常（它本来就取 `.result`） | `openvlab_collector.py`：改 `(data.get("result") or data).get("context")`，新旧结构兼容 |

### 20.2 Legend 桌面 UI（CDP）的深层限制

- 修复 1-4 后 CDP 9225 就绪，但页面是 Electron 壳页 `file:///E:/OpendVlab Legend/openvlab-legend/resources/app.asar/dist/index.html#/market`——**行情用虚拟列表渲染（Tailwind 12 列 grid、卡片随滚动回收），DOM 里没有 `<table>`**；`innerText` 兜底得到的是单字段平铺文本（"合约"与价格碎片分属不同 DOM 元素），无法映射到 `column_map`。
- 结论：**实时行情以 REST 通道为准**（openvlab.cn 后端与 Legend 客户端同源），桌面 UI 通道保持 CDP 探测/UIA 兜底，不再承担行情采集主路径。

### 20.3 实时行情打通（openvlab REST，用户指出的既有通道）

- `openvlab_collector.py` 走 openvlab.cn REST：`api/ctamap-all?add_overseas=true`（83 品种波动率地图）+ `api/dto/{CODE}`（**逐品种实时行情：最新价/买价/卖价/成交量/持仓量/涨跌幅**）+ `api/volatility-surface/{CODE}`（隐波曲面），并行 5 线程。
- 修复 5 后 `parse_dto_quote` 正常解析；**config.json `dto_limit` 30 → 83**（全量品种）。
- 行情经 fusion 落 `monitor.db` `quotes` 表 **cycle=99**（装置侧），与 Legend/THS 同通道，供量化跨源校验。

### 20.4 验证

- Legend 旧实例（无调试口）被自动结束 → 新实例以调试参数拉起，**CDP 9225 1 秒就绪**；`run.py --probe --legend` 正常输出页面与候选，不再报 NoneType。
- `run.py --once`：`openvlab={'ctamap': 83, 'quotes': 83, 'surfaces': 13}`（修复前 quotes 恒 0）；ths 同轮采到 5 条。
- 常驻 daemon（新代码 + dto_limit=83）：每轮 `quotes_by_source: {openvlab: 80}`（3 个品种偶发超时下轮自动补）；monitor.db cycle=99 实时入库（样例：沪锌2702 27640 / 豆油2611 8931 / PVC2612 5060 / 尿素2703 1843 / PTA2611 6436，ts 实时）。
- 装置 pytest **120/120 全绿**（新增 `test_parse_dto_quote_wrapped_result` result 包装结构用例）；selftest 16/16。
- 运维约定：daemon 一律**由 PyCharm 启停**（`python run.py` 无参数默认常驻）；本轮曾由命令行后台拉起验证，已全部停止。

### 20.5 遗留/可选

- Legend 桌面 UI 若需采集桌面专属视图（如自选/委托页）的虚拟列表数据，需针对该页面 DOM 写专用 JS 提取器（定位具体 section 的 grid 行）——当前实时监控不依赖，优先级低。
- openvlab dto 偶发 `read operation timed out`（09-09 已见），每轮重拉自动补全，无需处理。

---

## §二十一 第二十一轮：main/run 联动关闭拉起进程 + 同花顺期货通调试模式启动（2026-09-10）

> 用户要求：由 main/run 启动的软件进程，在父程序关闭后要同步关闭；随后明确 run.py 拉起的
> Legend 与同花顺都要以**调试模式**启动。

### 21.1 联动关闭（只杀"自己拉起的"）

| 父进程 | 启动的软件 | 关闭时机 | 实现 |
|--------|-----------|----------|------|
| run.py | Legend（CDP 9225）+ 同花顺 | `cmd_daemon` finally（PyCharm 停止/Ctrl+C） | `_launch_software` 每次 Popen 记录 PID 到 `_launched_pids`；新增 `_shutdown_started()` 逐个 `taskkill /F /T` |
| main.py | 同花顺 + 调试浏览器 | finally（正常/KeyboardInterrupt/--once）+ 看门狗 `os._exit(3)` 前 | `ths_app.kill_ths()`；调试浏览器原有 Job Object |
| main.py | 同花顺（**被强杀**兜底） | OS 级 | 同花顺挂 **Windows Job Object（KILL_ON_JOB_CLOSE）**，与调试浏览器同款——main 进程消失时系统自动终止同花顺 |

- **只杀自己 Popen 的实例**：已在运行的软件（手动开的）不记录 PID → 退出不误杀；main/run 各自记录，A 退出不影响 B 拉起的进程。
- 装置侧 `run.py` 新增 `_record_launched(p)` / `_shutdown_started()`（`import subprocess as _subprocess` 函数内延迟 import，因 subprocess 原本只在 `_launch_software` 局部导入）。

### 21.2 同花顺期货通调试模式启动（run.py，与 Legend 同款 restart 模式）

- **调试原理**（2026-09-10 实测）：`bin/workspace/DataCenter.xml` 的 `<Debug><Cef><Console enable="true"/></Cef>` 开启后重启 happ.exe 即弹独立 DevTools 窗口；CDP 端口对该客户端无效。
- **run.py 新增 `_ensure_ths_debug_mode()`**：幂等补写 DataCenter.xml（`Console enable=false→true` / Cef 块内插入 / 结构异常不强改；首次修改前备份 `.bak`；保留原换行风格），受 config.json `ths.debug_mode` 开关控制。
- **`_launch_software` 同花顺块改为与 Legend 同构**：进程在跑（可能为普通模式）且 `ths.restart_debug=true` → `taskkill happ.exe` 后以调试配置重启；未运行直接拉起；结束失败/开关关闭保持现状不误杀。
- config.json `ths` 段新增：`data_center_xml`（DataCenter.xml 路径）、`debug_mode: true`、`restart_debug: true`。

### 21.3 验证

- 装置 pytest **128/128** 全绿（新增 `_ensure_ths_debug_mode` 4 例：已开启不写回 / false→true 补丁+备份 / 开关关闭不动 / 文件缺失不抛；联动关闭 4 例：只记录自己拉起的 / 按 PID 逐个 taskkill / 无记录空操作 / taskkill 失败静默）；量化 903/903 全绿（ths_app kill_ths/PID 记录单测）。
- 实测真实 DataCenter.xml 已含 `enable="true"` → 启动时不写回，仅确认后拉起/重启。
- 修复过程中发现并修正：`_ensure_ths_debug_mode` 初版 `m.start(2)` off-by-one 会丢闭合引号 → 改用 `re.subn`。

---

## §二十二 第二十二轮：量化看板装置页签内嵌装置 Dashboard（看板合一）（2026-09-10）

> 用户要求"完成装置项目遗留"。§18.5 遗留"装置 dashboard 与量化实时报告.html 合一看板
> （当前各自独立服务）"在本轮实现——通过 iframe 在量化看板「装置健康详情」页签内嵌装置显示页。

### 22.1 实现（量化 report.py，装置侧无需改动）

- **`_device_panel_html()` 末尾新增"装置 Dashboard（看板合一）"段**：读取装置
  `E:\LHsystem\界面操作收集装置\config.json` 的 `http.serve_port`（默认 8790），内嵌 iframe
  `http://127.0.0.1:{port}/dashboard.html`（宽100%，高520px，暗色边框），显示完整装置显示页。
- **上方原有结构化 JSON 摘要（软件在线/数据源健康/采集概览/最近告警）保留**：装置 `--serve` 未启动时
  iframe 空白，结构化摘要兜底提示"先跑 start_all.bat"。
- 端口从装置 config.json 动态读（非硬编码），改端口自动跟随。

### 22.2 验证

- `report._device_panel_html()` 渲染输出含 iframe 标签（端口 8790）且保留结构化摘要（数据源健康等）。
- 量化 pytest 918 全绿（无回归）；`main.py --once` 冒烟 exit=0（未启动 --serve 时 iframe 空白属预期，
  上方摘要兜底，不影响看板）。

### 22.3 装置遗留状态更新

| 遗留 | 状态 |
|------|------|
| 看板合一（iframe/反向代理） | ✅ 本轮流地（量化侧 report.py iframe，端口动态读） |
| 阶段D：device_jykc 仓单因子 / Legend T链 PCR 接入量化综合分 | 待观察期 1-2 周后用户决策（§18.5 原样保留） |
| Legend 桌面专属视图 JS 提取器（自选/委托页） | 优先级低（实时行情已走 REST 通道），§20.5 保留 |
| openvlab dto 偶发超时 | 每轮自动补全，无需处理（§20.5 保留） |
| ctp_accounts 实盘账号 | 需用户手动加入 config（配置就绪） |
